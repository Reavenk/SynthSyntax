extern void LogTest(int loggedVal);
extern void EndTest();

entry DoTest()
{
	if(itStatement == 0)
		LogTest(10)
	else if(itStatement < 20)
	{
		LogTest(20);
		LogTest(30);
	}
	else if(itStatement == 30)
		LogTest(40);
	else
	{
		LogTest(20);
		LogTest(30);
	}
		
	EndTest();
}
