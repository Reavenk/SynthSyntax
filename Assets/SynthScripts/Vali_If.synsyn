extern void LogTest(int loggedVal);
extern void EndTest();
param int ifStatement = 0;

entry DoTest()
{
	if(itStatement == 0)
		LogTest(10)
	else
	{
		LogTest(20);
		LogTest(30);
	}
	
	if(itStatement >= 100)
	{
		LogTest(40);
		LogTest(50);
	}
	else
		LogTest(60);
		
	EndTest();
}
