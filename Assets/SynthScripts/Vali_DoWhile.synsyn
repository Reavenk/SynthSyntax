extern void LogTest(int loggedVal);
extern void EndTest();

entry DoTest()
{
	int i = 0;
	do
	{
		LogTest(10);
	}
	while(false)
	LogTest(20);
	
	int j = 20;
	do
	{
		j += 10;
		LogTest(j);
	}
	while(j < 100)
	
	EndTest();
}
